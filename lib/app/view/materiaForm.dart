import 'package:flutter/material.dart';

class cadastroMateria extends StatelessWidget {
  const cadastroMateria({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Cadastro de matérias'),
        backgroundColor: Color.fromRGBO(145, 14, 201, 100),
      ),
    );
  }
}
